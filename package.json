{
  "name": "bytecade-games",
  "version": "1.0.0",
  "description": "150 self-contained HTML5/React mini-games \u2014 no engine, no backend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/prerender.mjs && (node scripts/og-images.mjs || echo \"[og] generation skipped - committed cards will be used\")",
    "preview": "vite preview",
    "prerender": "node scripts/prerender.mjs",
    "og": "node scripts/og-images.mjs"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.0",
    "vite": "^5.4.0",
    "sharp": "^0.33.0"
  }
}